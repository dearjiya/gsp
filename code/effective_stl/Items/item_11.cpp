#include <doctest/doctest.h>

#include <fstream>
#include <list>
#include <vector>

namespace
{
}

/**
 *
 */
TEST_CASE("item 11. understand the legitimate uses of custom allocators")
{
  /**
   * 직접 구현해 보는 것이 이해하는데 가장 좋다. 코드도 쉽게 구할 수 있으므로 
   * 인터페이스에 맞춰서 구현해 보는 연습을 빠르게 해볼 수 있다. 
   */
  SUBCASE("write A custom allocator")
  {
  }
}
