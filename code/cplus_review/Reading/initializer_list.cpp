#include <doctest/doctest.h>

TEST_CASE("initializer_list")
{
	/**
	 * 무엇인가?  
	 *  생성자를 호출하여 클래스를 초기화 하는데 모든 초기화를 중괄호 표기로 
	 *  할 수 있도록 하기위해 std::initializer_list<T>와 컴파일러가 협업하여 
	 *  uniform initializer를 제공한다. 
	 * 왜 필요한가? 
	 *  이해가 어려운 이상한 문법 구조를 개선하고 배열처럼 초기화 하는 것이 
	 *  가능하도록 하기위해.
	 * 어떻게 쓰는가?
	 *  std::initializer_list<T>를 받는 생성자를 제공하면 쓸 수 있다.
	 */
	SUBCASE("intro")
	{
	}


}